<!DOCTYPE html>
<html>
<head>
    <title> Tabla de Posiciones - Signs </title>
    <style>
        body {
            background-color: #81BFE1;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        h1 {
            font-size: 80px;
			color: black;
			font-family: "Inter", sans-serif;
			text-align: center;
            vertical-align: text-top;
			margin: 20px;
			padding: 20px;
			line-height: 121px;
        }

        table {
            width: 80%;
            max-width: 800px;
            background-color: white;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }

        th, td {
            padding: 10px;
            text-align: center;
        }

        th {
            background-color: #81BFE1;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>

    <h1> Tabla de Posiciones </h1>
    <table>
        <tr>
            <th> Puesto </th>
            <th> Correo del usuario </th>
            <th> Puntos totales </th>
        </tr>
        <% pos = 1%>
        <% @users.each do |user|%>
            <tr>
                <td> <%= pos%> </td>
                <td> <%= user.name %> </td>
                <td> <%= (user.correct_responses_count) *10 %> </td>
            </tr>
        <% pos = pos + 1%>
        <% end%>
    </table>
</body>
</html>